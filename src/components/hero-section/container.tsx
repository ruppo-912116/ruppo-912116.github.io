import ProfileCard from "@/components/hero-section/profile-card";

export default function HeroSectionContainer() {

    return (
        <div className={"flex items-center justify-center min-h-screen max-h-screen bg-gray-100"}>
            <ProfileCard/>
        </div>
    )
}